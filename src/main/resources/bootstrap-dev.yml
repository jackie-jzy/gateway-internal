server:
  port: 8082
#
spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username:
        password:
        namespace:

  redis:
    # redis 单机环境配置
    host: localhost
    port: 6379
    password:
    database: 0

logging:
  config: classpath:logback-dev.xml